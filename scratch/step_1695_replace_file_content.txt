"  // ==========================================\n  // 4. COURSES CAROUSEL SLIDER CONTROLLER\n  // ==========================================\n  const sliderContainer = document.querySelector('.courses-slider-container');\n  if (sliderContainer) {\n    const track = sliderContainer.querySelector('.courses-slider-track');\n    const slides = sliderContainer.querySelectorAll('.course-slide');\n    const prevBtn = sliderContainer.querySelector('.prev-btn');\n    const nextBtn = sliderContainer.querySelector('.next-btn');\n    const pagination = sliderContainer.querySelector('.slider-pagination');\n\n    let currentIndex = 0;\n\n    // Create pagination dots\n    if (pagination && slides.length > 0) {\n      pagination.innerHTML = '';\n      slides.forEach((_, idx) => {\n        const dot = document.createElement('button');\n        dot.className = 'slider-dot' + (idx === 0 ? ' active' : '');\n        dot.setAttribute('aria-label', `Go to course ${idx + 1}`);\n        dot.addEventListener('click', () => {\n          currentIndex = idx;\n          updateSlider();\n        });\n        pagination.appendChild(dot);\n      });\n    }\n\n    const updateSlider = (smooth = true) => {\n      if (slides.length === 0) return;\n\n      // Handle transition style\n      if (smooth) {\n        track.style.transition = 'transform 0.6s cubic-bezier(0.25, 1, 0.5, 1)';\n      } else {\n        track.style.transition = 'none';\n      }\n\n      // Update active classes on slides\n      slides.forEach((slide, idx) => {\n        if (idx === currentIndex) {\n          slide.classList.add('active');\n        } else {\n          slide.classList.remove('active');\n        }\n      });\n\n      // Calculate centering translation\n      const activeSlide = slides[currentIndex];\n      const containerWidth = sliderContainer.offsetWidth;\n      const slideWidth = activeSlide.offsetWidth;\n      const slideLeft = activeSlide.offsetLeft;\n      \n      // Target position: center of container\n      const offset = (containerWidth - slideWidth) / 
<truncated 3315 bytes>